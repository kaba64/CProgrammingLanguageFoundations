#include <stdio.h>
int main(void){
  cahr name[50];
  int count=0; 
  scanf("%s",name);
  while(name[count]!='\0'){
    count++;
  }
  if(count%2==0){
    pintf("2");
      }else{
    pintf("1");
  }

  return 0;
}
